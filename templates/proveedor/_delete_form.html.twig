<form method="post" action="{{ path('app_proveedor_delete', {'id': proveedor.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminar este proveedor?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proveedor.id) }}">
    <button class="btn bg-gradient-danger"><i class="fa-solid fa-trash"></i> Eliminar</button>
</form>
