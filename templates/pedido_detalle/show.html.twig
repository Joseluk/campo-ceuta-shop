{% extends 'base.html.twig' %}

{% block title %}PedidoDetalle{% endblock %}

{% block body %}
    <h1>PedidoDetalle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pedido_detalle.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ pedido_detalle.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pedido_detalle_index') }}">back to list</a>

    <a href="{{ path('app_pedido_detalle_edit', {'id': pedido_detalle.id}) }}">edit</a>

    {{ include('pedido_detalle/_delete_form.html.twig') }}
{% endblock %}
