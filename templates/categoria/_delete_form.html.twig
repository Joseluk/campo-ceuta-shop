<form method="post" action="{{ path('app_categoria_delete', {'id': categorium.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminar esta categoría?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorium.id) }}">
    <button class="btn bg-gradient-danger"><i class="fa-solid fa-trash"></i> Eliminar</button>
</form>
