{% extends 'base.html.twig' %}

{% block title %}Categoria{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center py-3">Categoría</h1>

    <div class="table-responsive">
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categorium.nombre }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="d-flex justify-content-between my-3">
        <a href="{{ path('app_categoria_index') }}" class="btn btn-secondary" style="max-height: 40px;"><i class="fa-solid fa-backward"></i> Volver a la lista</a>
        <div>
            <a href="{{ path('app_categoria_edit', {'id': categorium.id}) }}" class="btn bg-gradient-warning"><i class="fa-solid fa-pen-to-square"></i> Editar</a>
            {{ include('categoria/_delete_form.html.twig', { 'class': 'd-inline-block' }) }}
        </div>
    </div>
</div>
{% endblock %}
