{% extends 'base.html.twig' %}

{% block title %}Editar Pedidos{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center py-3">Editar Pedidos</h1>

        {{ include('pedido/_form.html.twig', {'button_label': 'Actualizar '}) }}

        <div class="my-3">
            <a href="{{ path('app_pedido_index') }}" class="btn btn-secondary">
                <i class="fa-solid fa-backward"></i> Volver a la lista
            </a>
        </div>

        {{ include('pedido/_delete_form.html.twig') }}
    </div>
{% endblock %}

