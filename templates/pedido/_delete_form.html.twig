<form method="post" action="{{ path('app_pedido_delete', {'id': pedido.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminar este pedido?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pedido.id) }}">
    <button class="btn bg-gradient-danger"><i class="fa-solid fa-trash"></i> Eliminar</button>
</form>
