<form method="post" action="{{ path('app_stock_delete', {'id': stock.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminar este stock?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock.id) }}">
    <button class="btn bg-gradient-danger"><i class="fa-solid fa-trash"></i> Eliminar</button>
</form>
