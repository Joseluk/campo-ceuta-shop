{% extends 'base.html.twig' %}

{% block title %}Editar Stock{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center py-3">Editar Stock</h1>

    {{ include('stock/_form.html.twig', {'button_label': 'Actualizar'}) }}

    <div class="my-3">
        <a href="{{ path('app_stock_index') }}" class="btn btn-secondary">
            <i class="fa-solid fa-backward"></i> Volver a la lista
        </a>
    </div>

    {{ include('stock/_delete_form.html.twig') }}
</div>
{% endblock %}
