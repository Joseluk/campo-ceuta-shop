<aside class="sidenav navbar navbar-vertical navbar-expand-xs border-0 border-radius-xl my-3 fixed-start ms-3 " id="sidenav-main" data-color="succes">
    <div class="sidenav-header">
        <i class="fas fa-times p-3 cursor-pointer text-secondary opacity-5 position-absolute end-0 top-0 d-none d-xl-none" aria-hidden="true" id="iconSidenav"></i>
        <a class="navbar-brand m-0" href="{{ path('app_home') }}">
            <img src="{{ asset('assets/img/default-monochrome-black.svg') }}" class="navbar-brand-img h-100" alt="main_logo">
        </a>
    </div>
    <hr class="horizontal dark mt-0">
    <div class="collapse navbar-collapse w-auto left-menu-campoceuta" id="sidenav-collapse-main">
        <ul class="navbar-nav">
            {% for route in accesibleRoutes %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path(route.ruta) }}">
                        <div class="icon icon-shape icon-sm shadow border-radius-md bg-white text-center me-2 d-flex align-items-center justify-content-center position-relative">
                            <i class="fa-solid {{ route.icon }}"></i>
                            {% if route.ruta == 'app_mensajes' and noLeidosCount is defined and noLeidosCount > 0 %}
                                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="transform: translate(-50%, -30%) !important; font-size: 0.5rem;">
                            {{ noLeidosCount }}
                            <span class="visually-hidden">mensajes</span>
                                </span>
                            {% endif %}
                        </div>
                        <span class="nav-link-text ms-1">{{ route.nombre }}</span>
                    </a>
                </li>
            {% endfor %}
        </ul>

    </div>
    <div class="sidenav-footer mx-3 ">
        <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_logout') }}">
                        <div class="icon icon-shape icon-sm shadow border-radius-md bg-white text-center me-2 d-flex align-items-center justify-content-center">
                            <i class="fa-solid fa-right-from-bracket"></i>
                        </div>
                        <span class="nav-link-text ms-1">Cerrar sesión</span>
                    </a>
                </li>
        </ul>
    </div>
</aside>
